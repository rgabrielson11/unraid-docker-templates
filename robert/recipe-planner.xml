<?xml version="1.0"?>
<Container version="2">
  <Name>recipe-planner</Name>
  <Repository>ghcr.io/rgabrielson11/recipe-planner:latest</Repository>
  <Registry>https://ghcr.io/rgabrielson11/recipe-planner</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/rgabrielson11/recipe-planner/issues</Support>
  <Project>https://github.com/rgabrielson11/recipe-planner</Project>
  <Overview>Self-hosted, pantry-first weekly dinner planner for a household. Discovers recipes from HelloFresh and your local Mealie library, scores them against what you already have on hand, builds a combined shopping list, and can push it straight to a Bring! shopping list. Image is built automatically by GitHub Actions from https://github.com/rgabrielson11/recipe-planner on every push to master.</Overview>
  <Category>Tools: HomeAutomation:</Category>
  <WebUI>http://[IP]:[PORT:8111]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/rgabrielson11/unraid-docker-templates/master/robert/recipe-planner.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/walkxcode/dashboard-icons/main/png/mealie.png</Icon>
  <ExtraParams/>
  <PostArgs/>
  <CPUset/>
  <DonateText/>
  <DonateLink/>
  <Requires>A running Mealie instance on your network (for recipe storage/import), and optionally a Bring! account (for shopping-list push).</Requires>
  <Config Name="WebUI Port" Target="8111" Default="8111" Mode="tcp" Description="Port the web UI listens on inside the container" Type="Port" Display="always" Required="true" Mask="false">8111</Config>
  <Config Name="Database" Target="/app/data" Default="/mnt/user/appdata/recipe-planner/data/db" Mode="rw" Description="SQLite database storage — back this up before updating" Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/recipe-planner/data/db</Config>
  <Config Name="YAML Config" Target="/app/app/data" Default="/mnt/user/appdata/recipe-planner/backend/app/data" Mode="rw" Description="Hand-editable YAML config: pantry staples, recipe sources, package sizes, rejection reasons, cooking vocabulary" Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/recipe-planner/backend/app/data</Config>
  <Config Name="Mealie Base URL" Target="MEALIE_BASE_URL" Default="" Mode="" Description="Your self-hosted Mealie instance URL, no trailing slash (e.g. http://192.168.1.50:9000)" Type="Variable" Display="always" Required="true" Mask="false"></Config>
  <Config Name="Mealie API Token" Target="MEALIE_API_TOKEN" Default="" Mode="" Description="Service account token — generate in Mealie -&gt; Profile -&gt; API Tokens" Type="Variable" Display="always" Required="true" Mask="true"></Config>
  <Config Name="Bring! Email" Target="BRING_EMAIL" Default="" Mode="" Description="Bring! account email — leave blank to disable the Push to Bring! button" Type="Variable" Display="always" Required="false" Mask="false"></Config>
  <Config Name="Bring! Password" Target="BRING_PASSWORD" Default="" Mode="" Description="Bring! account password — leave blank to disable the Push to Bring! button" Type="Variable" Display="always" Required="false" Mask="true"></Config>
  <Config Name="Homepage Dashboard URL" Target="RECIPE_PLANNER_URL" Default="" Mode="" Description="LAN URL for Homepage dashboard auto-discovery, e.g. http://192.168.1.50:8111 (optional)" Type="Variable" Display="advanced" Required="false" Mask="false"/>
  <Config Name="Database Path" Target="DATABASE_PATH" Default="/app/data/recipe_planner.db" Mode="" Description="SQLite file path inside the container — change only if you also change the Database volume mapping" Type="Variable" Display="advanced" Required="true" Mask="false">/app/data/recipe_planner.db</Config>
</Container>
